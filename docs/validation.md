# Validation evidence

## Shell and Settings decluttering · 2026-09-15

Implemented the annotated screenshot's continuous sidebar/titlebar, 12px workspace corner, centered Settings column, and removal of generic page descriptions, sidebar status text and footer copy. Pause/resume now lives in General; About Sentry retains engine details and backup/background limitations. Concurrent logo and control updates in the shared checkout were preserved.

Final hidden Electron evidence: `outputs/declutter/1789454353595/results.json` and adjacent PNGs. Captured General at 2048×1118 logical pixels (the requested 1117px height rounds at host scaling), 1180×780, 820×600 and 590×390 CSS pixels at 200% zoom, dark/light themes, paused state, empty Destinations, expanded About, Quit confirmation and empty Overview. Inspected desktop, compact, light/dark, confirmation and full native 200% captures. Settings heading, tabs and fields align; the shell corner and continuous rail match the reference. Compact content scrolls without horizontal document overflow. The 200% image uses native `capturePage` because Playwright's zoomed capture cropped the surface.

The real renderer/preload/worker path confirmed pause and resume persistence in an isolated profile. ArrowRight/Home changed Settings tabs correctly; Quit opened its warning dialog and Escape dismissed it. No renderer errors; test window visibility remained false. No existing user process or backup repository was modified.

Typecheck, lint, production build and `git diff --check` passed. `bun test`: 15 passed, 0 failed, 84 assertions, including real encrypted backup/restore and failure recovery. UI source audit: zero errors; its one existing search-field outline warning has a visible `.search-box:focus-within` replacement. Final controls use the concurrently updated shared primitives. Physical titlebar interaction, screen readers, and installed-package behavior were not retested for this presentation change.

Windows 11 x64 build 26200; Bun 1.3.6; host Node 25.2.1. Electron 40.10.6 embeds Node 24.15.0, Chromium 144.0.7559.236 and SQLite 3.51.3. Engines: restic 0.19.1 and rclone 1.75.1. Fixtures use isolated directories and never existing backup repositories.

## Data safety

Final `bun test`: **15 passed, 0 failed, 84 assertions**, across five files in 58.34 seconds. TypeScript, ESLint and the production build passed. The separately executed Node service fixture passed all **12 scenarios**.

The engine prototype ran before application integration. Automated engine tests cover byte-for-byte multi-root restore, incremental changes/deletion, empty files/directories, Unicode, 335-character Windows paths, renamed files, wrong passwords, pinned retention followed by recovery, locked modified files, changing content after scanning, unexpected include-rule arrivals, cancellation during an actual write, stale-lock cleanup and integrity checks. A real rclone local backend validates transport discovery without claiming Google account success.

The service suite runs the actual worker service and SQLite through Node. Twelve scenarios pass: independent destination commits; incremental local success with another destination unavailable; failed-only retry; complete restore; honest collision results; real retention removal with retained recovery; wrong credentials; redacted diagnostics; queued cancellation/persistence; interrupted-job recovery; missed schedule catch-up; and recovery without the original application database. Latest service evidence: `outputs/service-fixture/run-wIC1or/results.json`.

Automation tests cover DST/month end, missed schedules, durable weather deduplication, stale/invalid/no-coverage responses, simulation separation, OAuth loopback state rejection and PKCE exchange, refresh, timeout, cancellation and quota parsing. A live NWS request for Dallas succeeded on September 15, 2026. OAuth provider responses in protocol tests are fixtures; live Google account operations remain unverified.

## Native application

Hidden/offscreen native Electron validation used a fresh profile, actual encrypted repository, source files and real jobs. It verified context isolation, renderer sandboxing and no renderer Node access. The local backup and verified restore crossed renderer/preload/main/worker/SQLite/engine boundaries. Two identically named files from different roots restored byte-for-byte.

The first native run found Bun's bundled `__dirname` pointing at source paths. Main now resolves packaged entrypoints from `app.getAppPath()/dist`. Development and packaged discovery must both pass before acceptance.

Rendered evidence covers empty and protected Overview, plan editor, Restore, Settings, dark/light, 820×600 and 200% zoom. The first pass found a misleading no-plan tray label and excessive empty-state instructions; both were repaired. Light metadata contrast was raised to at least 4.72:1 on the rail. UI lists use bounded pages, dialogs trap and return focus, and reduced motion removes nonessential transitions.

Final packaged evidence is `outputs/native-qa/1789453037142/results.json`, with eight adjacent PNG screenshots. The plan was saved through the actual editor; a named pinned backup, full byte-for-byte restore and nonempty sample recovery completed. Twenty-four Tab presses stayed inside the Radix dialog. No page errors occurred, both compact/200% layouts had no horizontal overflow, and reduced motion was enabled. All test windows remain hidden; this establishes actual native rendering, not physical monitor interaction or assistive-technology acceptance.

## Windows candidate

`release/Sentry Setup 0.1.0.exe` is an unsigned NSIS per-user installer. The unpacked packaged executable, bundled engine discovery, fresh writable profile and DPAPI credential path were exercised. Installer execution and uninstall were not exercised.

- Installer SHA-256: `9f1fa1e26cc4b34d03172b676f015bcdca925edf2ed5245e0e45bf9896ef868c`.
- Packaged `app.asar` SHA-256: `18a01392c08793f80eba7d5e2cb31596d6e7594e17980d157907c8e42b6044df`.
- Resource metadata identifies Sentry 0.1.0; packaged restic matches the downloaded executable byte-for-byte. Approved PNG originals, multi-resolution ICO and engine license notices are included.

The full process tree measured **429.68 MiB** with a hidden retained renderer and **278.44 MiB** in the tray over approximately 33 seconds per state. These exceed the initial memory targets. Software rendering reduced tray working set by 19.2%; the final idle CPU counter did not advance within the sampling resolution. Hidden rendering is a proxy, not a visible-window benchmark. See [performance](performance.md) for per-process values, methodology and limitations.

The native workflow screenshots and idle benchmark precede one final service change: active jobs now overlay their live progress on persisted history when publishing state and reading Activity. This repairs a stale “Scanning” label without writing every progress event to SQLite. The final package was rebuilt and exercised by the [stress validation](stress-validation.md); benchmark evidence retains the exact earlier package hash.

The final package backed up **4,001 files / 137,760,328 bytes** in **13.177 seconds**, with zero skipped files and renderer errors. Fifty-eight navigation/keyboard interactions spanned Scanning, Backing up and Verifying; the slowest measured interaction took **46.30 ms**. Sampled active process-tree working set peaked at **673.74 MiB**. Evidence: `outputs/stress/1789453517170/results.json` and `busy-engine.png`. This is one hidden/offscreen local fixture, not a large-repository or cloud soak test. All test-owned processes exited.

## Limits that require external evidence

- Production Google Desktop OAuth client/consent configuration and real account transfer/reconnect/quota/long-transfer interruption.
- Physical external-drive removal/reinsertion, changed drive letters and network-share interruption. Directory disappearance/reconnection and read-only volume identity were exercised.
- Real sleep/resume and Windows login across reboot. Schedule decisions and missed-run persistence were exercised.
- Elevated VSS, application-consistent or full-system recovery are not exposed in this build.
- Actual sudden power loss, hardware/storage-controller behavior, very large real repositories, and multi-day soak operation.
- Installer is unsigned. A packaged executable smoke test does not establish trusted-publisher signing or a clean-machine installation lifecycle.

Performance measurements and their process-tree boundaries are maintained separately in [performance.md](performance.md).

## In-app branding, September 15, 2026

Used the supplied logo unchanged as `public/sentry-mark.png` for the title-bar brand, loading state and welcome state. Moved the Sentry brand to the top-left title-bar area above sidebar navigation. The existing working-tree removal of the File protection subtitle is preserved.

Validation: `bun run typecheck`, `bun run lint`, `bun test` (15 passed, 84 assertions), and `bun run build` passed. A hidden/offscreen Electron smoke check confirmed the brand text is Sentry, both rendered logo assets load, and the window stays hidden. Screenshot inspected: `outputs/branding/overview.png`; readback: `outputs/branding/results.json`. No installer or release was produced for this change.
`nBrand placement follow-up: lowered the title-bar brand by 10 CSS pixels after user feedback. Production build and hidden Electron branding smoke check passed; refreshed outputs/branding/overview.png.

## Shared control polish, September 15, 2026

Implemented themed Radix dropdowns across Settings, plan editing, and Restore; animated switches for boolean settings; custom selection checkboxes; consistent scrollbar, hover, press and disabled treatments. Existing theme tokens, domain values and request handlers remain the authority.

Validation: typecheck, lint, production build and all 15 data-safety tests (84 assertions) passed. UI source audit: zero errors; its one warning is the existing search input outline removal, paired with the visible `.search-box:focus-within` outline.

Hidden/offscreen Electron control acceptance passed in `outputs/native-qa/1789454485468/results.json`: native visibility false, no page errors, dropdown Escape returns focus, keyboard typeahead selects Dark, Space changes the battery switch and changes it back, and reduced motion yields 0s track/thumb transitions. Inspected Settings menus in light and dark at 1180×780 and dark at 820×600 (150% display scaling); compact document/body widths both equal 820. Screenshots 09, 11 and 12 show coherent controls without major visual defects. Also inspected the plan editor from `outputs/native-qa/1789454395187/02-plan-editor-dark.png`.

The broader QA runs reached real backup, restore and sample recovery before their added control checks timed out: first on an incorrect hint selector, then on a typeahead focus race in the harness. Both harness issues were repaired; the affected control sequence passed independently using `outputs/controls-qa/check.ts`. The full QA script was not rerun after that focused confirmation. No screen-reader, physical-device, installer or release validation is claimed by this control pass.

## Sidebar and software updates · 2026-09-15

Settings is anchored at the bottom of the sidebar. Update checks run after startup and every six hours in packaged builds. The sidebar stays quiet until a newer release is available, then offers Download update, download progress, retry on download failure, and Restart to update. Downloads and installation require separate user actions. Development and hidden QA do not contact the feed or install anything.

Native main owns electron-updater and merges its state into the existing validated bridge. The worker drains accepted requests and refuses update shutdown while jobs are active or queued. Installation starts only after clean worker exit. Downgrades, prereleases, and installation on ordinary quit are disabled.

Release prerequisite: publish the Windows NSIS installer, blockmap and generated latest.yml together to IEver3st/Sentry GitHub Releases. The build now configures this provider; no release was published. Actual download, installed upgrade, signing and public feed remain unverified. Installed electron-updater 6.8.9 types were checked because current online documentation describes a newer major API: https://www.electron.build/docs/features/auto-update/.

Evidence: outputs/sidebar-updates/results.json and 1180-/820-*.png cover hidden/current, available, downloading, ready, and retry states at 1180x780 and 820x600. Settings remained below the update control, with no page overflow; ready action keyboard focus is visible. These update versions are isolated QA fixtures, not published releases. Full hidden native backup/restore QA passed in outputs/native-qa/1789454550417/results.json. Earlier data-safety suite: 15 passed; updater unit tests: 3 passed; build passed. Source audit: no errors, one existing outline warning with a visible search-container focus replacement.

Concurrent engine work changed during validation. Latest whole-repository typecheck/lint currently reports unrelated capture.ts DatabaseSyncOptions errors and incomplete service methods/imports. Earlier checks passed before those edits. Final whole-repository status must be reconciled with that work; no unrelated engine fixes were made here.

## Settings overhaul and UI scaling · 2026-09-15

Implemented a section rail, section headings, aligned preference rows and trailing switches across Settings. General exposes 80/90/100/110/125/150/175/200 percent app scaling and reset. Engine settings parsing migrates old profiles to 100 percent; validated IPC rejects out-of-range/non-integer values. Main applies confirmed scale through Electron page zoom on load and state updates. No repository or credential operations were moved into the renderer.

Hidden/offscreen Electron evidence: `outputs/settings-qa/1789454812674/`. Inspected General dark/light, Destinations empty/unconfigured Google, Weather and Maintenance at 1180×780, General at 820×600 and 125/200 percent zoom. Native capture requires invalidating the offscreen surface after zoom; early Playwright screenshots cropped the zoomed surface and were superseded by native captures. Final measurements show no horizontal document overflow at 1180, 944, 820 and 410 CSS pixels. At 200 percent, section navigation wraps and content scrolls. Selection/reset, tab arrow navigation and renderer reload passed. `restart.json` confirms 1.25 native zoom after a complete app/worker restart using the same isolated profile. QA windows remained hidden; no primary-monitor windows were shown. No page errors.

Settings migration/range tests: 2 pass, 14 assertions. The initial full test run passed all 15 automation/backup data-safety tests and two updater tests; one concurrently edited updater test initially failed, then all three updater tests passed on targeted rerun. Build passed. Initial typecheck/lint passed; final current-tree checks are blocked by concurrent database-capture work: unsupported `enableExtensions` options in `src/engine/capture.ts` lines 22/26 and unused `captureDatabases` import in `src/engine/service.ts`. Those files were not repaired as part of this UI task. UI source audit: zero errors, one existing outline warning; `.search-box:focus-within` supplies the visible replacement.

Limits: no screen-reader walk, physical native-titlebar interaction, new installer validation, live Google connection, or fault-injected settings-save failure. Other working-tree changes continued during this task, so initial build/check evidence is a point-in-time result, not approval of all concurrent edits.

Restart safety follow-up: all 13 service integration scenarios passed in outputs/service-fixture/run-i5P8Zg/results.json, including queued-job rejection and idle preparation rejecting subsequent requests. A focused latest-source guard check (outputs/update-guard.ts) also passed with queued work absent from the visible recent-history list, active work, and idle shutdown. The worker uses the complete queued-job query, not the renderer's capped history. Subsequent concurrent typecheck failures also include the new main-process ResourceProcess diagnostics typing; these remain outside this task.

## Blank UI scale selector correction · 2026-09-15

Reproduced the reported blank label in hidden Electron by saving the valid non-preset value 115: native zoom was accepted but Radix had no matching option (`outputs/scale-fix/1789455259985/`). General now includes the confirmed saved percentage among its sorted presets and uses 100 for a missing legacy value. Reset uses the same resolved value. No stored preference is silently rounded.

Confirmation: `outputs/scale-fix/1789455292688/scale.png` shows 115%. Native UI checks passed display of 115%, selection of 125%, and reset to 100%. Settings tests passed (including non-preset contract acceptance); typecheck, build, targeted ESLint and UI source audit passed. Full lint encountered a concurrent unrelated `no-unsafe-finally` error in engine/service.ts line 1239. Existing user app process and profile were not modified; verification used an isolated hidden profile. The screenshot alone does not establish which value was in the user's running profile.

## Overview charts and customization · 2026-09-15

Implemented daily backup activity, a five-outcome completion ring, optional processed-data charts, 7/30-day selection, and saved choices for charts, protection ledger and recent activity. Existing settings IPC owns persistence; old settings remain compatible through an optional validated overview field. No dependency or engine/catalog aggregation path was added. Chart scope is explicitly the latest 100 jobs, filtered to backup completion dates in the selected local-calendar period. Partial, failed, cancelled and interrupted runs never contribute successful-backup bytes. Processed bytes are labeled as repeated per run/destination, not repository storage.

Protection summary and active jobs cannot be hidden. Paused state links to Settings; gaps link to Backup Plans. An unavailable destination retains its unavailable label even if its last successful copy predates the failure. Long unbroken plan/destination names now wrap instead of overflowing the ledger.

Evidence:
- Baseline: `outputs/native-qa/1789454592298/03-overview-protected-dark.png`. The incumbent full QA later stopped on its Settings Select-focus assertion; that unrelated flow was not changed here.
- Native integration: `outputs/overview-qa/1789454962724/results.json`. Hidden/offscreen Electron 40, sandbox and context isolation enabled, renderer Node access absent. An actual encrypted backup succeeded. Customization through the dialog persisted through settings IPC and page reload; the range changed to 30 days; focus returned to Customize after save. Native 820×600 and 200% zoom (590×390 CSS) had no horizontal document overflow. No page errors. Native script is isolated at `outputs/overview-qa.ts`.
- Final visual confirmation: `outputs/overview-visual/`, generated by `outputs/overview-fixture.mjs` using the actual built renderer with an isolated headless fixture. Reviewed dark/light populated charts, processed bytes, chart focus, customization, failed save/retry, no history, all sections hidden/paused, no plans, failed/running backups, and long names. Viewports: 1180×780, 820×600 and 590×390. Both document and workspace overflow assertions passed. Compact dialog save actions remain visible and lower choices are keyboard reachable. Save failure preserves confirmed preferences; successful retry clears the error. Fixtures never enter production state.
- Fresh pixel-first review found no blockers and identified small annotations and cramped dialog insets; these were repaired with 11px annotations and the existing scrollable dialog-body/persistent-footer pattern. A long-name stress case exposed ledger overflow, repaired and confirmed in `13-long-names-failed.png`. Final reviewed screenshots have no remaining blocker or major visual defect.

Checks: `bun run typecheck` passed; `bun run build` passed (existing bundle-size/Zod annotation warnings); focused ESLint passed for Overview, App and contracts; `bun test tests/overview.test.ts tests/engine.test.ts` passed 5 tests covering outcome truth, calendar boundaries, settings compatibility, actual encrypted backup/restore, independent catalog recovery, locked-file/incomplete handling and interrupted/disconnected recovery. Source audit: 0 errors, one existing search-field outline warning with a visible parent `:focus-within` replacement. `git diff --check` passed.

Full-project lint remains blocked by the concurrent engine change at `src/engine/service.ts:1239` (`no-unsafe-finally`). No unrelated engine edits were made for this UI task. This evidence does not establish packaged-release behavior, screen-reader operation, live cloud authorization, or physical drive reconnection. No release or publication was performed.

## Toggle redesign · 2026-09-15

Shared switches now use a 38x22 track with a 16px thumb and equal 3px outer insets at both ends. Off is a neutral outlined track; on is solid blue with a white thumb. Focus, disabled, reduced-motion and forced-color styles remain supported. Checkbox behavior and saved preferences are unchanged.

Hidden native Settings/General inspection: outputs/toggle-polish/results.json and dark/light-1180/820-on/off.png. Verified on/off states in both themes at 1180x780 and 820x600, keyboard Space changes, exact thumb/track geometry, no page overflow, disabled state and reduced-motion transitions. Theme selection was exercised through the real UI. Build and typecheck passed; two settings tests passed; source audit found no errors (existing search-field outline warning has focus-within replacement). Full lint is blocked by an unrelated no-unsafe-finally error in the concurrently edited engine/service.ts. Forced-color styling was added but not visually inspected.


## Settings diagnostics workspace - 2026-09-15

Implemented on rebuild/electron, preserving concurrent Settings, onboarding, recovery and background-service work. Settings > Diagnostics follows the supplied resource-monitor reference structure in Sentry's theme: footprint, host/engine health, resource timeline, process ledger, filterable session events and expandable recent jobs.

Behavior and boundaries:
- A validated diagnostics-snapshot request collects Electron metrics in main and reads worker counters/queue metadata in the engine. It bypasses the mutation queue and does not reserve or start backup work. Desktop-worker diagnostics requests have a five-second timeout; concurrent snapshot requests share one collection.
- The panel polls every two seconds while live and visible, stops on pause/unmount/hidden document, and retains up to 450 samples covering 15 minutes of this panel session. Initial CPU readings are labeled warming up. The timeline uses actual capture timestamps and adaptive, labeled scales.
- The session log holds the newest 500 safe request/job-transition events with source, severity, operation, time, duration or job ID. Initial job history is not fabricated as live transitions. Names, request arguments, credentials and raw errors never enter this log. Recent job details display the existing local history separately.
- Export includes the existing redacted engine metadata plus current counters and session events. If the worker fails, an explicitly partial report still exports. Reports omit account details, paths, passwords, file contents and raw engine errors. No native save dialog was opened in QA.
- CPU and memory cover Electron processes plus the worker, not restic/rclone subprocesses or disk I/O. Summed working sets/RSS may count shared memory more than once. Resource collection is not proof of successful protection. No persistent raw debug log, hardware temperature, disk throughput or whole-system process scan is claimed.

Validation:
- bun test: 26 passed, zero failures, including encrypted snapshots, independent recovery, retention, interrupted/locked files, Windows paths and local rclone transport. Diagnostics retention, transition deduplication and metadata-isolation tests also passed independently after implementation.
- Build passed with the existing dependency annotation and bundle-size warnings. Diagnostics-owned files passed ESLint; frontend source audit reported zero errors/warnings. Initial full typecheck passed. Later repository-wide checks were blocked by concurrent edits: no-unsafe-finally in the recovery sample cleanup in src/engine/service.ts; App.tsx unused imports/overview prop mismatches; qa-onboarding.ts environment typing. These are outside the diagnostics hunks and were preserved.
- Final native evidence: outputs/diagnostics-qa/1789455585992/results.json. Electron window stayed hidden/offscreen, zero page errors. Successful fixture backup, expandable job details, failed-request filtering/disclosure, no-match state, pause, redacted JSON export, keyboard Settings navigation, worker termination/partial health and partial export all passed. The isolated profile/repository stayed under outputs.
- Rendered and inspected dark/light at 1180x780, compact 820x600, and 200% zoom (590x390 effective CSS viewport); no document/body horizontal overflow. Final screenshots use Electron capturePage with stayHidden after invalidation, avoiding cropped or stale Playwright captures. See 02-timeline-processes.png, 03b-expanded-event.png, 04b-job-details.png, 05-light-paused.png and 06-compact-* under the final evidence directory.
- Independent pixel review found no blocker/major defect in readable initial captures. Repaired crowded numeric columns, low-CPU chart readability and evidence gaps for light/high-zoom/log detail states. Final captures were visually inspected.
- Earlier evidence runs exposed QA-only issues: cropped/stale screenshots, a newly introduced onboarding entry screen, and an unreliable async polling predicate. The harness was adapted; no production fixture data was added.
- Not verified: native save-dialog interaction, screen-reader use, 15-minute soak, installed/service-host diagnostics, Google Drive or physical drive failure. No commit, release, version bump or existing user-app restart was performed.

## Backup Plans, Restore and Activity redesign · 2026-09-15

Scope: the three routes circled in the user's screenshot. Visual contract: `docs/workspace-redesign.md`. Kept the existing shell, Windows typography, dark/light tokens, Radix controls, validated IPC and engine-owned catalog/backup operations. Other active changes in this checkout were preserved.

- Backup Plans now has a compact summary, searchable/filterable plan library, aligned per-destination rows, current availability, and a separate last-complete-copy timestamp. Expansion retains source paths, retention, enable/disable, duplicate, checkpoint and confirmed deletion. Current destination attention/unavailability takes precedence over an older successful job.
- Restore now shows the repository/version/recovery sequence, actionable repository setup, preparation guidance, repository and plan filters, dated versions and the existing file browser. Loading, empty snapshots and errors are distinct; retry reloads the catalog. Files from a previous request are cleared during loading and restore is disabled while files load or an error is present. Separate-folder/no-overwrite behavior and actual verification remain intact.
- Activity now has outcome and operation filters, search, expandable outcomes and useful empty-state actions. Filters explicitly apply to the current 50-operation page. Failed/partial/cancelled/interrupted operations are grouped as incomplete, never successful. Read errors retain the previous rows and offer retry.
- Motion: short page entrance, rotating disclosure chevrons, 1px button lift and restrained spring-like press/settle. No motion dependency or idle loop. Reduced motion removes transitions and entrances.

Rendered evidence: `outputs/workspace-qa/1789455847035/`, with `results.json` and `final-results.json`. Actual Electron stayed hidden/offscreen (`visible: false`) using an isolated QA profile and repository. Inspected empty pages, populated plans, source-error attention, no search matches, no snapshots, selected-file restore dialog, successful history, expanded failed job and incomplete filter. Dark/light 1180×780, light 820×600, and final dark 200% scale at 590×390 effective CSS pixels were inspected. No document or workspace horizontal overflow. At high zoom the page scrolls and the restore action remains reachable; `final-restore-200percent-action.png` shows it.

Native behavior: created an actual encrypted repository and backup; selected `notes.txt` through the redesigned file browser and restored through the dialog with no overwrite. Restored contents matched the original. A source removed from the isolated fixture after plan validation produced a real failed backup; outcome filters included/excluded it correctly. Plan search/no-match/reset, activity search/no-match/reset, repository navigation, keyboard opening/dismissing the plan dialog, and reduced-motion CSS were exercised. No renderer page errors.

QA harness corrections: the first failure fixture attempted to save an already-missing source, which validation correctly rejected. It was changed to remove an owned fixture file after plan save. A history-count assertion was corrected to include the real automatic prune job. Manual webContents zoom was reset by settings updates, so the final batch used persisted `uiScale: 200`; Electron capturePage produced uncropped images. Final evidence supersedes the initially mislabeled `restore-200percent.png` and `activity-200percent.png` captures.

Checks: `bun test` passed 28 tests (including real encrypted backup/restore, independent catalog recovery, retention, locked/interrupted/disconnected work, Unicode paths and local rclone transport). Final typecheck and build passed; existing Zod annotation and bundle-size warnings remain. Changed renderer files passed ESLint. The UI audit had zero errors and two existing outline warnings with parent focus treatment. `git diff --check` passed. Full-project lint was attempted and is currently blocked by an unrelated concurrent `scripts/qa-recovery.ts:29` require-import rule error; that script was not edited here.

Visual review: the new first-view layouts provide clear actions and useful backup/recovery context without fabricated history. Corrected singular labels, shortened empty-state headings and reduced recovery pane height after inspection. Final inspected captures have no blocker or major visual defect. Screen-reader operation, live cloud recovery, physical drive reconnection and packaged-release behavior were not revalidated. No commit, publication, release, or user-app restart was performed.

## Onboarding redesign and native verification · 2026-09-15

Implemented a modular first-run flow in `src/renderer/onboarding/`: typed step registry/validation, controller, focused step components, independent custom SVG artwork/background, and scoped styling. Existing users retain the workspace. Dismissal persists through validated engine settings; empty Overview can reopen Guided setup. The recovery link opens Settings > Destinations directly. Destination credentials remain in the existing ephemeral DestinationForm; the worker still owns repository creation, source validation, plan storage and backup execution.

Visual authority: the user's correction rejected the full-height split blueprint. Read Apple’s official Onboarding, Design principles, Layout and Motion guidance, plus Switchboard’s actual onboarding screenshot and source. `docs/onboarding-design.md` records the final centered composition, dimensional Sentry artwork, compact horizontal progression, concise copy and adjacent primary actions. The screenshot critic found no additional blocker/major issue and identified excess Review spacing, which was reduced. Removed the noninteractive heading focus outline and redundant outer footer while repository creation owns its controls. Interactive controls keep their visible keyboard focus.

Final proof: `outputs/onboarding-qa/1789456261095/results.json` and named PNG captures, from actual hidden/offscreen Electron (`visible: false`, `offscreen: true`) with a dedicated fixture profile. Inspected welcome at 1180×780, 820×600 and 2560×1390; source-validation failure, destination creation/selection, manual schedule, engine rejection of a missing source, review, light compact review, and saved-plan handoff. Native capturePage produced uncropped high-zoom evidence at 590×390 effective CSS pixels; `09b-review-actions-light-200-percent.png` proves the footer remains reachable after scrolling. No document horizontal overflow or renderer page errors. Reduced motion recorded zero running animations. The global visibility marker now initializes from document.hidden as well as tracking later visibility changes.

Behavior proof: persisted Set up later across reload; reopened Guided setup; recovery link selected the Destinations tab; keyboard activation advanced from welcome and moved focus to the step heading; Radix selected Manual; backward editing retained the plan name/destination; canceling Leave setup preserved the draft. A missing source was rejected by the real engine without saving a plan. A valid plan saved with zero jobs, then Start first backup produced an actual successful encrypted backup (snapshot `3de60dd92d5bdc0b07f5414aac133e8f123bb07926a1b2642ca3bff133230a1e`). Reload after setup returned to the workspace. No fixture data was written into the user's profile.

Checks: strict TypeScript, changed-file ESLint, build and onboarding model tests passed. The full 28-test suite and 13 service integration scenarios passed earlier in this task, including real backup/restore, catalog-independent recovery, retention, interrupted/disconnected work, wrong-password rejection and Unicode paths. Full-project lint passed during development; the latest whole-checkout run encountered a concurrent unrelated `scripts/qa-background.ts:34` no-constant-condition error. No unrelated source was changed to clear it. Existing build warnings concern Zod annotations and the renderer chunk size. The UI source audit has zero errors and one inspected outline warning, limited to the noninteractive programmatically focused step heading.

Harness corrections: awaited terminal backup state by polling actual IPC results instead of passing an async predicate to waitForFunction. Persisted uiScale was used because state updates overwrite manual webContents zoom. Native captures wait for an invalidated paint after resizing. Earlier failed/mislabeled harness outputs are superseded by the final run above.

Boundaries: no physical native picker interaction, complete screen-reader walk, live Google authorization, physical drive reconnection, packaged installation or release verification was performed. Existing native titlebar branding can clip at high app zoom; setup content and controls reflow and remain usable. No user-app restart, commit, push or release.

Final checkout verification: the subsequent whole-project bun run lint passed after the concurrent script edit settled. Changed-file git diff --check also passed.

## Native titlebar integration fix · 2026-09-15

The startup overlay used the canvas color (#101319) and a 40-DIP height while the CSS titlebar uses the rail color and 34 CSS pixels. That painted a separate native-control rectangle over the workspace border until a later state update. Native appearance now uses one function at window creation, renderer load, published desktop/service state and OS theme changes. The overlay matches the dark/light rail exactly and its height follows the saved renderer scale (floor of 34 × scale). Existing Windows caption behavior remains native.

Validation: typecheck, full lint, build and five focused settings/updater tests passed. Hidden/offscreen native evidence is in `outputs/titlebar-qa/1789456398326/results.json`. Verified matching colors, no overlap with the workspace border, native Window Controls Overlay geometry, reload synchronization and OS light/dark changes. Tested 80%, 100%, 125% and 200% scale; the reported native overlay area stays 34 CSS pixels high at each scale. Window visibility remained false; no page errors or horizontal overflow. Dark/light renderer captures were visually inspected and the workspace top line is continuous.

Capture boundary: Electron capturePage omits the OS-drawn caption glyphs and their hover/snap surfaces. Native colors and geometry were verified through the actual overlay configuration and Window Controls Overlay API; visible OS hover/snap rendering was not exercised. Existing user windows were not restarted. A full Sentry restart loads this main-process fix. Staged and concurrent work was preserved.

## Recovery, checkpoints and independent protection · 2026-09-15

Implemented file history with verified text/image previews, side-by-side versions and deleted-file recovery; snapshot content changes; named expiring checkpoints; same-capture copies between independent repositories; rotating scheduled recovery drills; opt-in protection discovery and gap review; backup on observed drive arrival and finished-drive feedback; recovery-kit export and fresh-catalog/password practice; Explorer history registration; optional Windows service setup; and live SQLite / crash-consistent VSS capture options. Advanced plan controls remain collapsed under Capture & automation. Settings groups readiness and integration under Recovery. Full behavior and operational limits are in `docs/recovery.md`.

Engine evidence: `outputs/recovery-fixture/run-vrdmOy/results.json` passed ten integrated scenarios using real encrypted repositories. It confirms original capture timestamps and tags across copies, recovery of old bytes after live sources change, modified/deleted/added content comparison, missing-file history, pending-copy pruning/pin guards, checkpoint retention, a verified 12-file drill with scratch cleanup, fresh-catalog recovery and wrong-password/target-collision rejection, a live SQLite WAL capture restored to its logical original path, opt-in discovery without plan mutation, an emulated drive arrival that queues only once and rejects a different repository, finished-drive state, and a scheduled drill only for the untested copy without repeating inside its interval. The earlier catalog-independent practice deliberately deleted local catalog tables; the schedule fixture rebuilds that catalog before testing scheduling. These are disposable files under outputs, not production history.

Background and queue boundaries: two Node named-pipe tests passed mutual authentication, wrong-token rejection, exclusion of native UI actions, client-close/reconnect and single-profile host ownership. A third focused queue test passed cancellation of a retried copy, release of its ancestor retention holds and reacquisition of a hold on a later retry (`outputs/recovery-state/run-U7u3X0`). Windows token ACL setup uses .NET access-control APIs directly, avoiding inherited PowerShell module-path incompatibility. `bun run test:recovery` now runs all three boundary tests and the ten integrated scenarios under Node, whose SQLite implementation matches the worker. Bun's named-pipe test crash was isolated to the test runtime; that harness was moved to Node.

Native UI: `outputs/recovery-qa/1789457278650/results.json` and eight PNGs prove actual hidden/offscreen Electron history preview, comparison, selected-version restore with exact bytes, snapshot changes, Recovery settings and password-clearing practice recovery. Views were checked at 1180×780, 820×600 and light mode at 200% scale (590×390 CSS pixels), with no horizontal document overflow or renderer errors. Screenshots were inspected. The fresh visual critic's final pass on the preceding equivalent layout (`outputs/recovery-qa/1789456464309`) found no blocker or major defect; indistinguishable version labels, long-path visibility, singular change labels and missing high-scale evidence were addressed. Final small history changes retain the selected repository, allow retrying the same query and reopen repeated Explorer requests.

Independent host: `outputs/background-qa/1789457202851/results.json` proves a hidden Electron service host with no BrowserWindow, authenticated desktop connection, backup completion after the desktop closes, reopening against the same catalog and verified sample recovery. The client quit path schedules app.quit after disconnect to avoid a reentrant quit hang. Remote state uses the shared native appearance function. A disconnected client explicitly reports last-known state. Unexpected service worker exit terminates the host for SCM recovery. The native C# host compiled into `dist/service/SentryService.exe`; the exported setup PowerShell script passed parser validation. Export does not install a service or serialize credentials.

Checks: the broad `bun test` run passed 28 tests and `bun run test:service` passed 13 service scenarios (`outputs/service-fixture/run-BtwNnJ/results.json`). Final typecheck, full-project lint and build passed; existing Zod annotation and renderer chunk-size warnings remain. The focused retry-hold test passed after its final fix. `git diff --check` passed with only repository LF/CRLF notices. Build includes the native service assets. No new full-suite run was needed after those bounded fixes.

Proof boundaries: Windows SCM installation, signed-out/reboot behavior, physical drive removal/reconnection, Explorer registration in an installed app, elevated VSS and real Google-account copying were not exercised. VSS is not arbitrary application consistency; SQLite databases are consistent independently. Sample recovery is not a full-repository restore. No service was installed and no release was published. Concurrent staged/unstaged work was preserved. The shared checkout moved to main during concurrent work; remaining uncommitted changes were placed on `rebuild/recovery-features` without resetting files or rewriting commits.

## Main branch consolidation · 2026-09-15

At the user's explicit request, `main` now contains the histories of `rebuild/electron` and `feature/rebuild/onboarding-workspaces` and is the local working branch and GitHub default. AGENTS.md and current project documentation direct future work to main. Existing branch refs and backup/legacy files were preserved.

Waited for the ongoing titlebar and recovery/background tasks to complete. The recovery task created `rebuild/recovery-features` from main under its older branch instructions and left its changes uncommitted. After its completion, switched the shared checkout back to main without losing edits, reconciled that branch, and included the finished recovery, native-window, test and documentation changes in the final integration commit. No active branch tip remained outside main.

Final combined-tree checks: `bun run typecheck`, `bun run lint`, `bun run build`, `bun test` (28 passed), and `bun run test:recovery` (three background/copy-state boundary tests plus ten recovery scenarios) all passed. Recovery evidence: `outputs/recovery-fixture/run-cn8HNK/results.json`. This successful run supersedes the earlier in-progress recovery run that reported a busy repository. Build warnings remain limited to existing Zod annotations and renderer chunk size. The agent's hidden native evidence and operational limits above remain applicable; no release or service installation was performed during consolidation.
