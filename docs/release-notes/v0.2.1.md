Fixed the Windows taskbar icon and application identity so Sentry uses its own icon instead of Electron's default icon. Development and installed copies now have separate taskbar identities.

This update preserves the Drive features and background performance improvements from v0.2.0.

The Windows installer is unsigned.
